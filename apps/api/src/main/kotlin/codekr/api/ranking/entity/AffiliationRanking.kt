package codekr.api.ranking.entity

/**
 * 소속 랭킹의 최소 인원이자 **점수를 세는 인원** (#400, 기획서 6절).
 *
 * 두 값을 같게 둔다. 한 명짜리 학교가 1등으로 올라오면 그 순위표는 아무 말도 하지
 * 않으므로 최소 인원이 필요하고, 점수를 세는 인원이 그보다 크면 **인원이 곧 점수**가
 * 되어 사람을 끌어모으는 순위표가 된다.
 */
const val MIN_AFFILIATION_MEMBERS = 5
