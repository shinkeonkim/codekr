/**
 * 사이트의 정본 주소와 이름 (#277, #284).
 *
 * **`NEXT_PUBLIC_` 이 아니다.** 그 접두사는 값을 브라우저 번들에 박아 넣는데, 이 값이
 * 필요한 곳은 서버가 메타데이터를 만들 때뿐이다. 박아 넣으면 환경마다 이미지를 따로
 * 빌드해야 하고, 같은 이미지를 공개 주소와 내부 주소로 함께 서비스할 수 없다
 * (`shared/config` 가 API 주소를 비워 둔 것과 같은 이유다).
 *
 * 주소는 **퓨니코드**다 (ADR-0009). `metadataBase` 가 `URL` 을 받는 순간 어차피
 * 퓨니코드로 정규화되고, 구글은 둘을 같은 것으로 본다.
 *
 * ## 이 값은 빌드 시점에 정해진다
 *
 * 루트 레이아웃의 `metadata` 는 정적이라 **빌드할 때 계산된다.** 컨테이너에
 * `SITE_ORIGIN` 을 넣어도 이미 구운 페이지의 태그는 바뀌지 않는다 (직접 확인했다 —
 * 다른 값을 넣고 띄워도 `og:url` 이 그대로였다).
 *
 * 그래서 차트에 환경 변수를 두지 않았다. **하는 일이 없는 설정은 있는 것이 더 나쁘다.**
 * 지금 공개 주소가 하나뿐이라 기본값으로 충분하고, 주소가 둘 이상이 되면 그때
 * `generateMetadata` + `headers()` 로 옮긴다 — `sitemap.ts` 가 이미 그 방식이다.
 * 그 대가는 모든 화면이 요청 시점 렌더가 되는 것이라, 필요해지기 전에는 치르지 않는다.
 */
export const SITE_ORIGIN = process.env.SITE_ORIGIN ?? "https://xn--hy1by51c.kr";

/**
 * 사람에게 보이는 이름. **여기만 한글이다** (ADR-0009).
 *
 * 공유 카드의 주소는 퓨니코드로 뜨지만, 카드의 이름만은 한글로 읽히게 한다.
 */
export const SITE_NAME = "코드.kr";

export const SITE_DESCRIPTION =
  "알고리즘, 자료구조, SQL, 네트워크, 운영체제, 시스템 설계까지. 실시간 채점 과정을 보며 문제를 풉니다.";
